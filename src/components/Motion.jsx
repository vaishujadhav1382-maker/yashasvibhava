// import React from 'react';

// // Simple motion wrapper that applies basic CSS animations
// export const motion = {
//   div: ({ children, className, initial, animate, transition, whileInView, viewport, whileHover, whileTap, ...props }) => (
//     <div className={`${className || ''} animate-fade-in`} {...props}>
//       {children}
//     </div>
//   ),
  
//   button: ({ children, className, whileHover, whileTap, ...props }) => (
//     <button className={`${className || ''} transition-transform hover:scale-105 active:scale-95`} {...props}>
//       {children}
//     </button>
//   )
// };

// export default motion;